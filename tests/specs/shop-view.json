[
  {
    "name": "🛒 Shop opens with catalogue choices",
    "steps": [
      { "send": { "callback": "shop:view" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Welcome to the tiny treasure shop!" } }] }
    ]
  }
]
